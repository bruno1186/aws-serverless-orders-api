{
  "name": "aws-serverless-orders-api",
  "version": "1.0.0",
  "description": "API serverless de pedidos na AWS (Lambda + API Gateway + DynamoDB)",
  "main": "src/handlers/createOrder.js",
  "scripts": {
    "test": "jest --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.588.0",
    "@aws-sdk/lib-dynamodb": "^3.588.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "aws-sdk-client-mock": "^4.0.0"
  }
}
